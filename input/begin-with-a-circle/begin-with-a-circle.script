[let script = _debug_script_state()]

Let's begin with a circle. [browser: let c1 = circle(50,"red")]  [td: ...]

Let's make it a bit smaller. [scale(c1, .5, 2)]

Let's add a blue one. [let c2 = circle(50,"blue")]

Let's add an even smaller red one. [let c3 = circle(30,"red")]

From the top. [RESET()]